<Window x:Class="Pharmacy_Manage.GUI.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông tin sản phẩm" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Border Background="#FFFFFF" CornerRadius="15" BorderBrush="#E2E8F0" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#3182CE" CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock x:Name="txtTitle" Text="THÊM THUỐC MỚI" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Button Content="✕" Click="BtnCancel_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" 
                            Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" Cursor="Hand"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="25,20,25,0">
                <TextBlock Text="Tên thuốc (*)" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,5"/>
                <Border CornerRadius="8" BorderBrush="#CBD5E0" BorderThickness="1" Background="#F7FAFC" Margin="0,0,0,15">
                    <TextBox x:Name="txtTenSP" Height="35" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                </Border>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Số lượng tồn (*)" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,5"/>
                        <Border CornerRadius="8" BorderBrush="#CBD5E0" BorderThickness="1" Background="#F7FAFC">
                            <TextBox x:Name="txtTonKho" Height="35" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Đơn vị tính" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,5"/>
                        <Border CornerRadius="8" BorderBrush="#CBD5E0" BorderThickness="1" Background="#F7FAFC">
                            <TextBox x:Name="txtDonVi" Height="35" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Giá bán (VNĐ)" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,5"/>
                <Border CornerRadius="8" BorderBrush="#CBD5E0" BorderThickness="1" Background="#F7FAFC" Margin="0,0,0,15">
                    <TextBox x:Name="txtGiaBan" Height="35" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                </Border>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Hạn sử dụng" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpHanDung" Height="35" FontSize="14"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Ngày nhập" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpNgayNhap" Height="35" FontSize="14"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <Border Grid.Row="2" Background="#F7FAFC" CornerRadius="0,0,15,15" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Button Content="Hủy bỏ" Click="BtnCancel_Click" Width="100" Height="40" Background="#A0AEC0" Foreground="White" BorderThickness="0" Margin="0,0,10,0" FontSize="14" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="Lưu Dữ Liệu" Click="BtnSave_Click" Width="130" Height="40" Background="#48BB78" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>