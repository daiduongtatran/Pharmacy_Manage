<UserControl x:Class="Pharmacy_Manage.GUI.QLkhachhang_view"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100" Background="#F4F7F6">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThai}" Value="Đã thanh toán">
                    <Setter Property="Foreground" Value="#48BB78"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Chờ thanh toán">
                    <Setter Property="Foreground" Value="#D69E2E"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Đã hủy">
                    <Setter Property="Foreground" Value="#E53E3E"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" Opacity="0.05" ShadowDepth="1"/>
            </Border.Effect>
            <StackPanel>

                <UniformGrid Columns="3" Margin="0,0,0,15">
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="Tổng hóa đơn" Foreground="#718096" FontWeight="SemiBold" FontSize="13"/>
                        <TextBlock x:Name="txtTongHoaDon" Text="0" FontSize="24" FontWeight="Bold" Foreground="#2C9BB3" Margin="0,2,0,0"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="Tổng doanh thu (VNĐ)" Foreground="#718096" FontWeight="SemiBold" FontSize="13"/>
                        <TextBlock x:Name="txtTongDoanhThu" Text="0" FontSize="24" FontWeight="Bold" Foreground="#48BB78" Margin="0,2,0,0"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Chưa thu tiền (VNĐ)" Foreground="#718096" FontWeight="SemiBold" FontSize="13"/>
                        <TextBlock x:Name="txtChuaThu" Text="0" FontSize="24" FontWeight="Bold" Foreground="#E53E3E" Margin="0,2,0,0"/>
                    </StackPanel>
                </UniformGrid>

                <Separator Background="#EDF2F7" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="🔍 Tra cứu (Mã HĐ, Tên khách, SĐT)" Foreground="#718096" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtTimKiem" Height="35" Padding="10,0" FontSize="13" VerticalContentAlignment="Center" TextChanged="Filter_Changed">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1" Background="White">
                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                    </Border>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,15,0" Width="150">
                        <TextBlock Text="📅 Lọc theo ngày:" Foreground="#718096" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpLocNgay" Height="35" SelectedDateChanged="Filter_Changed"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Width="150">
                        <TextBlock Text="📊 Trạng thái:" Foreground="#718096" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cbTrangThai" Height="35" VerticalContentAlignment="Center" FontSize="13" SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="Tất cả" IsSelected="True"/>
                            <ComboBoxItem Content="Đã thanh toán"/>
                            <ComboBoxItem Content="Chờ thanh toán"/>
                            <ComboBoxItem Content="Đã hủy"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" Opacity="0.05" ShadowDepth="1"/>
            </Border.Effect>
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="📜 LỊCH SỬ GIAO DỊCH &amp; HÓA ĐƠN" FontSize="16" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,10"/>

                <DataGrid x:Name="dgHoaDon" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="White" BorderThickness="0" RowHeight="40" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF2F7">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã HĐ" Binding="{Binding MaHD}" Width="70">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Ngày Lập" Binding="{Binding NgayLap, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
                        <DataGridTextColumn Header="Khách Hàng" Binding="{Binding TenKhachHang}" Width="160"/>
                        <DataGridTextColumn Header="SĐT" Binding="{Binding SDT}" Width="100"/>

                        <DataGridTextColumn Header="Tiền Dịch Vụ" Binding="{Binding TongTienDichVu, StringFormat={}{0:N0}}" Width="100"/>
                        <DataGridTextColumn Header="Tiền Sản Phẩm" Binding="{Binding TongTienSanPham, StringFormat={}{0:N0}}" Width="110"/>

                        <DataGridTextColumn Header="Tổng Cần Thu" Binding="{Binding TongThanhToan, StringFormat={}{0:N0}}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#E53E3E"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Trạng Thái" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TrangThai}" Style="{StaticResource StatusTextStyle}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Thao tác" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Chi tiết" Background="#EDF2F7" Foreground="#2C9BB3" FontWeight="Bold" BorderThickness="0" Padding="10,4" Cursor="Hand" Click="BtnChiTiet_Click">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>