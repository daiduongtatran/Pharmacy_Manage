<UserControl x:Class="Pharmacy_Manage.GUI.DatLich_view"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="#F4F7F6">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.5*"/>
            <ColumnDefinition Width="6.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="20" Margin="0,0,15,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="📝 Tiếp nhận bệnh nhân" FontSize="20" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20"/>

                <TextBlock Text="Số điện thoại (*)" Foreground="#718096" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,15">
                    <TextBox x:Name="txtSDT" Padding="10,0,40,0"/>
                    <Button Content="🔍" HorizontalAlignment="Right" Width="40" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnTimBN_Click" ToolTip="Tìm bệnh nhân cũ"/>
                </Grid>

                <TextBlock Text="Họ và Tên (*)" Foreground="#718096" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtHoTen" Margin="0,0,0,15"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Giới tính" Foreground="#718096" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cbGioiTinh" Height="40" VerticalContentAlignment="Center" FontSize="14">
                            <ComboBoxItem Content="Nam" IsSelected="True"/>
                            <ComboBoxItem Content="Nữ"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Năm sinh" Foreground="#718096" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtNamSinh"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Lý do khám / Triệu chứng" Foreground="#718096" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLyDo" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="10" Margin="0,0,0,15"/>

                <TextBlock Text="Phòng khám" Foreground="#718096" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="cbPhongKham" Height="40" VerticalContentAlignment="Center" FontSize="14" Margin="0,0,0,25">
                    <ComboBoxItem Content="Phòng Khám Nội 1" IsSelected="True"/>
                    <ComboBoxItem Content="Phòng Khám Nội 2"/>
                    <ComboBoxItem Content="Phòng Răng Hàm Mặt"/>
                    <ComboBoxItem Content="Phòng Tai Mũi Họng"/>
                </ComboBox>

                <Button Content="LƯU &amp; PHÁT SỐ THỨ TỰ" Height="50" Background="#2C9BB3" Foreground="White" FontSize="15" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnDatLich_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="15" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                    <TextBlock Text="⏳ Danh sách hàng chờ hôm nay" FontSize="20" FontWeight="Bold" Foreground="#2D3748" VerticalAlignment="Center"/>
                    <Button Content="Làm mới" HorizontalAlignment="Right" Background="#EDF2F7" Foreground="#4A5568" Padding="15,5" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <DataGrid x:Name="dgDanhSachCho" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="White" BorderThickness="0" RowHeight="45" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF2F7">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#E53E3E"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Họ và Tên" Binding="{Binding HoTen}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Giới/Tuổi" Binding="{Binding GioiTinhTuoi}" Width="80"/>
                        <DataGridTextColumn Header="Phòng Khám" Binding="{Binding PhongKham}" Width="150"/>

                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#D69E2E"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Thao tác" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Gọi khám" Background="#48BB78" Foreground="White" BorderThickness="0" Padding="10,5" Margin="0,0,5,0" Cursor="Hand" Click="BtnGoiKham_Click">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                        <Button Content="Hủy" Background="#FFF5F5" Foreground="#E53E3E" BorderThickness="0" Padding="10,5" Cursor="Hand" Click="BtnHuyKham_Click">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>