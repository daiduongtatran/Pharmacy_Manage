<Window x:Class="Pharmacy_Manage.GUI.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="MediTrack Admin" Height="800" Width="1280"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="SidebarBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0AEC0"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="25,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderThickness="4,0,0,0" BorderBrush="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2D3748"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#F7FAFC" CornerRadius="20" BorderBrush="#E2E8F0" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1A202C" CornerRadius="20,0,0,20">
                <StackPanel Margin="0,40,0,0">
                    <TextBlock Text="PHARMACY PRO" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,50"/>

                    <Button Style="{StaticResource SidebarBtnStyle}" Content="🏠  Trang chủ" Tag="0" Click="Menu_Click"/>
                    <Button Style="{StaticResource SidebarBtnStyle}" Content="📦  Kho thuốc" Tag="1" Click="Menu_Click"/>
                    <Button Style="{StaticResource SidebarBtnStyle}" Content="🧾  Hóa đơn" Tag="2" Click="Menu_Click"/>
                    <Button Style="{StaticResource SidebarBtnStyle}" Content="👥  Nhân viên" Tag="3" Click="Menu_Click"/>

                    <Button Style="{StaticResource SidebarBtnStyle}" Content="🚪  Đăng xuất" Margin="0,300,0,0" Click="Logout_Click" Foreground="#F56565"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <TabControl x:Name="MainTabControl" Background="Transparent" BorderThickness="0">
                    <TabControl.Template>
                        <ControlTemplate TargetType="TabControl">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </ControlTemplate>
                    </TabControl.Template>

                    <TabItem>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="30">
                                <TextBlock Text="Tổng quan hệ thống" FontSize="28" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,25"/>

                                <Grid Margin="0,0,0,30">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="#FFF5F5" CornerRadius="15" Padding="25" Margin="0,0,10,0" Cursor="Hand" MouseDown="CardUrgent_Click">
                                        <StackPanel>
                                            <TextBlock Text="Cần xử lý gấp" Foreground="#C53030" FontSize="16" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="txtUrgentTotal" Text="0 Thuốc" FontSize="32" FontWeight="Bold" Foreground="#E53E3E" Margin="0,10"/>
                                            <TextBlock Text="Bao gồm thuốc hết hạn &amp; tồn thấp" Foreground="#F56565"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Background="#E6FFFA" CornerRadius="15" Padding="25" Margin="10,0,0,0">
                                        <StackPanel>
                                            <TextBlock Text="Doanh thu hôm nay" Foreground="#2C7A7B" FontSize="16" FontWeight="SemiBold"/>
                                            <TextBlock Text="12.500.000 đ" FontSize="32" FontWeight="Bold" Foreground="#319795" Margin="0,10"/>
                                            <TextBlock Text="+15% so với hôm qua" Foreground="#38B2AC"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <Border Background="White" CornerRadius="15" Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="Biểu đồ doanh thu tuần" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                                        <lvc:CartesianChart Height="300" Series="{Binding RevenueSeries}">
                                            <lvc:CartesianChart.AxisX>
                                                <lvc:Axis Title="Ngày" Labels="{Binding ChartLabels}"/>
                                            </lvc:CartesianChart.AxisX>
                                        </lvc:CartesianChart>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem>
                        <Grid Margin="30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                                <TextBlock Text="Quản lý kho thuốc" FontSize="28" FontWeight="Bold" Foreground="#2D3748" VerticalAlignment="Center"/>
                                <Button Content="+ Thêm thuốc mới" Click="BtnAdd_Click" Background="#48BB78" Foreground="White" Padding="20,10" Margin="30,0,0,0" FontWeight="Bold" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Content="✏️ Sửa" Click="BtnUpdate_Click" Background="#4299E1" Foreground="White" Padding="20,10" Margin="10,0,0,0" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Content="🗑️ Xóa" Click="BtnDelete_Click" Background="#F56565" Foreground="White" Padding="20,10" Margin="10,0,0,0" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>

                            <Border Grid.Row="1" Background="White" CornerRadius="15" Padding="10">
                                <DataGrid x:Name="dgSanPham" AutoGenerateColumns="False" CanUserAddRows="False" 
                                          IsReadOnly="True" Background="White" BorderThickness="0" 
                                          FontSize="14" RowHeight="45" GridLinesVisibility="Horizontal">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Mã" Binding="{Binding MaSP}" Width="60"/>
                                        <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenSP}" Width="*"/>
                                        <DataGridTextColumn Header="Tồn kho" Binding="{Binding TonKho}" Width="100"/>
                                        <DataGridTextColumn Header="Hạn dùng" Binding="{Binding HanDung, StringFormat='dd/MM/yyyy'}" Width="150"/>
                                        <DataGridTextColumn Header="Ngày nhập" Binding="{Binding NgayNhap, StringFormat='dd/MM/yyyy'}" Width="150"/>
                                        <DataGridTextColumn Header="Đơn vị" Binding="{Binding DonVi}" Width="100"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>