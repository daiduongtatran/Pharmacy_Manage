<Window x:Class="Pharmacy_Manage.GUI.Hotro_window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="400" Width="500">

    <Border CornerRadius="20" Background="White" Margin="20" MouseDown="Window_MouseDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.ColumnSpan="2"
        CornerRadius="20,20,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#3B68A8" Offset="0"/>
                        <GradientStop Color="#2C9BB3" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="15,0">

                        <TextBlock Text="📞 Hỗ trợ"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="White"/>

                        <TextBlock Text="  Liên hệ admin khi gặp sự cố"
                       Foreground="White"
                       Opacity="0.85"
                       Margin="10,5,0,0"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                Content="✕"
                Width="35"
                Height="35"
                Margin="0,5,10,5"
                Background="Transparent"
                BorderThickness="0"
                Foreground="White"
                Cursor="Hand"
                Click="Close_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Opacity" Value="0.8"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="1"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                    </Button>

                </Grid>
            </Border>


            <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="20,5,20,20">

                <TextBlock Text="Danh sách Admin"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,10,0,40"/>

                <ScrollViewer Margin="0,50,0,0" VerticalScrollBarVisibility="Hidden">
                    <ItemsControl x:Name="AdminList" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Border Background="#F9F9F9"
                                        CornerRadius="10"
                                        Padding="15"
                                        Margin="0,0,0,15">

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>

                                        <Ellipse Width="50" Height="50" Fill="#3498DB"/>

                                        <StackPanel Grid.Column="1" Margin="10,0">
                                            <TextBlock Text="{Binding Ten}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding SDT}" Foreground="Gray"/>
                                            <TextBlock Text="{Binding Email}" Foreground="Gray"/>
                                        </StackPanel>

                                        <Button Grid.Column="2"
                                                Content="Copy"
                                                Background="#2C9BB3"
                                                Foreground="White"
                                                Click="Copy_Click"
                                                Tag="{Binding SDT}"
                                                Cursor="Hand"/>
                                    </Grid>
                                </Border>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Grid>
        </Grid>
    </Border>
</Window>